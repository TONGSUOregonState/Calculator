# Physics Pipeline - React + Vite

现代化的物理公式流水线计算器，使用 React 18 + Vite 构建。

## 特性

- ⚛️ React 18 with Hooks
- ⚡ Vite 开发服务器
- 📦 模块化组件架构
- 🎨 现代化 CSS 样式
- 🔬 10+ 物理公式支持

## 项目结构

```
physics-pipeline-react/
├── src/
│   ├── components/      # React 组件
│   │   ├── Block.jsx
│   │   ├── Category.jsx
│   │   ├── Sidebar.jsx
│   │   ├── TopBar.jsx
│   │   └── Results.jsx
│   ├── data/           # 数据定义
│   │   └── formulas.js
│   ├── utils/          # 工具函数
│   │   ├── calculator.js
│   │   └── formatter.js
│   ├── App.jsx         # 主应用组件
│   ├── App.css
│   ├── main.jsx        # 入口文件
│   └── index.css
├── index.html
├── package.json
└── vite.config.js
```

## 开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

## 使用说明

1. 在左侧公式库中选择需要的公式
2. 为每个 Block 输入参数（支持直接输入数字或引用其他 Block 的输出）
3. 点击 "Run Pipeline" 执行整个流水线
4. 或点击单个 Block 的 "Calculate" 按钮单独计算

## 引用格式

- `#1(v)` - 引用第1个 Block 的输出变量 v
- `block1(x)` - 引用第1个 Block 的输出变量 x
- `10` - 直接输入数字
